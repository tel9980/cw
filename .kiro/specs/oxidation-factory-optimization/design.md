# 设计文档：氧化加工厂财务助手系统优化

## 1. 系统架构设计

### 1.1 整体架构

系统采用分层架构设计：

```
┌─────────────────────────────────────────┐
│         用户交互层 (CLI/GUI)             │
│  - 命令行菜单                            │
│  - 文件选择对话框                        │
│  - 进度显示                              │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         业务逻辑层                       │
│  - 订单管理模块                          │
│  - 对账引擎                              │
│  - AI分类器                              │
│  - 报表生成器                            │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         数据访问层                       │
│  - 飞书API封装                           │
│  - Excel读写                             │
│  - 本地缓存管理                          │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         数据存储层                       │
│  - 飞书多维表格                          │
│  - 本地配置文件                          │
│  - 备份文件                              │
└─────────────────────────────────────────┘
```

### 1.2 核心模块

#### 1.2.1 订单管理模块 (OrderManager)
- 负责加工订单的创建、查询、更新
- 支持多种计价单位
- 管理订单与收款的关联关系

#### 1.2.2 对账引擎 (ReconciliationEngine)
- 实现灵活的多对多对账逻辑
- 支持部分对账和余额管理
- 生成对账报告

#### 1.2.3 AI分类器 (IndustryClassifier)
- 基于GLM-4的智能分类
- 行业规则库
- 学习和优化机制

#### 1.2.4 报表生成器 (ReportGenerator)
- 生成行业专用报表
- 支持多维度统计
- Excel导出功能

## 2. 数据模型设计

### 2.1 飞书多维表格结构

#### 表1：加工订单表 (Processing_Orders)

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| 订单编号 | 文本 | 唯一标识 | PO20260209001 |
| 客户名称 | 文本 | 关联往来单位 | 华为科技 |
| 订单日期 | 日期 | 下单日期 | 2026-02-09 |
| 物品名称 | 文本 | 加工物品描述 | 铝合金外壳 |
| 计价单位 | 单选 | 件/条/只/个/米长/米重/平方 | 件 |
| 数量 | 数字 | 加工数量 | 1000 |
| 单价 | 数字 | 单位加工费 | 2.5 |
| 订单金额 | 公式 | 数量×单价 | 2500 |
| 已收款金额 | 数字 | 累计收款 | 1500 |
| 未收款金额 | 公式 | 订单金额-已收款 | 1000 |
| 工序明细 | 多行文本 | 包含的工序 | 喷砂、氧化、封孔 |
| 外发工序 | 多选 | 委外工序 | 喷砂、拉丝 |
| 外发成本 | 数字 | 外发加工费 | 300 |
| 订单状态 | 单选 | 待生产/生产中/已完工/已结算 | 已完工 |
| 备注 | 多行文本 | 其他说明 | 客户要求黑色氧化 |

#### 表2：日常台账表 (Daily_Ledger) - 扩展现有表

新增字段：
| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| 关联订单号 | 关联 | 关联到加工订单表 | PO20260209001 |
| 对账状态 | 单选 | 未对账/部分对账/已对账 | 已对账 |
| 对账金额 | 数字 | 本次对账金额 | 1500 |
| 对账时间 | 日期时间 | 对账操作时间 | 2026-02-09 14:30 |
| 对账备注 | 文本 | 对账说明 | 订单PO001部分收款 |

#### 表3：往来单位表 (Business_Partners)

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| 单位名称 | 文本 | 标准名称 | 华为技术有限公司 |
| 单位类型 | 单选 | 客户/供应商/两者 | 客户 |
| 别名列表 | 多行文本 | 一行一个别名 | 华为\n华为科技\nHuawei |
| 联系人 | 文本 | 主要联系人 | 张经理 |
| 联系电话 | 电话 | 联系方式 | 13800138000 |
| 应收余额 | 数字 | 当前应收款 | 5000 |
| 应付余额 | 数字 | 当前应付款 | 0 |
| 备注 | 多行文本 | 其他信息 | VIP客户 |

#### 表4：对账记录表 (Reconciliation_Records)

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| 对账编号 | 文本 | 唯一标识 | REC20260209001 |
| 对账日期 | 日期时间 | 操作时间 | 2026-02-09 14:30 |
| 往来单位 | 关联 | 关联往来单位表 | 华为技术有限公司 |
| 银行流水ID | 关联 | 关联台账表 | 多个 |
| 订单ID | 关联 | 关联订单表 | 多个 |
| 对账金额 | 数字 | 本次对账总额 | 3000 |
| 对账类型 | 单选 | 收款对账/付款对账 | 收款对账 |
| 操作人 | 人员 | 执行对账的用户 | 张会计 |
| 备注 | 多行文本 | 对账说明 | 订单PO001和PO002合并收款 |

#### 表5：费用分类规则表 (Category_Rules)

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| 关键词 | 文本 | 匹配关键词 | 三酸 |
| 费用分类 | 单选 | 目标分类 | 原材料-三酸 |
| 优先级 | 数字 | 匹配优先级 | 10 |
| 是否启用 | 复选框 | 规则状态 | ✓ |
| 备注 | 文本 | 规则说明 | 硫酸、硝酸、盐酸 |

### 2.2 本地配置文件

#### oxidation_config.json
```json
{
  "pricing_units": ["件", "条", "只", "个", "米长", "米重", "平方"],
  "outsourced_processes": ["喷砂", "拉丝", "抛光"],
  "material_types": ["三酸", "片碱", "亚钠", "色粉", "除油剂", "挂具"],
  "bank_accounts": {
    "G银行基本户": {
      "type": "对公账户",
      "has_invoice": true,
      "is_cash": false
    },
    "N银行": {
      "type": "现金账户",
      "has_invoice": false,
      "is_cash": true
    },
    "微信": {
      "type": "现金账户",
      "has_invoice": false,
      "is_cash": true
    }
  },
  "default_categories": {
    "收入": ["加工费收入", "其他收入"],
    "支出": [
      "原材料-三酸",
      "原材料-片碱",
      "原材料-亚钠",
      "原材料-色粉",
      "原材料-除油剂",
      "挂具",
      "外发加工-喷砂",
      "外发加工-拉丝",
      "外发加工-抛光",
      "房租",
      "水电费",
      "工资",
      "日常费用",
      "其他支出"
    ]
  }
}
```

## 3. 核心算法设计

### 3.1 灵活对账算法

```python
def flexible_reconciliation(bank_records, orders, partner_name):
    """
    灵活对账算法：支持多对多匹配
    
    参数:
        bank_records: 银行流水列表
        orders: 订单列表
        partner_name: 往来单位名称
    
    返回:
        匹配结果列表
    """
    # 1. 筛选该往来单位的未对账记录
    unmatched_bank = [r for r in bank_records 
                      if r.partner == partner_name and r.reconcile_status != '已对账']
    unmatched_orders = [o for o in orders 
                        if o.customer == partner_name and o.unpaid_amount > 0]
    
    # 2. 按日期排序
    unmatched_bank.sort(key=lambda x: x.date)
    unmatched_orders.sort(key=lambda x: x.date)
    
    # 3. 智能匹配策略
    matches = []
    
    for bank_rec in unmatched_bank:
        remaining_amount = bank_rec.amount
        matched_orders = []
        
        # 3.1 精确匹配：金额完全相等
        for order in unmatched_orders:
            if abs(order.unpaid_amount - remaining_amount) < 0.01:
                matched_orders.append({
                    'order': order,
                    'amount': remaining_amount
                })
                remaining_amount = 0
                break
        
        # 3.2 部分匹配：一笔流水对应多个订单
        if remaining_amount > 0:
            for order in unmatched_orders:
                if order.unpaid_amount <= remaining_amount:
                    matched_orders.append({
                        'order': order,
                        'amount': order.unpaid_amount
                    })
                    remaining_amount -= order.unpaid_amount
                    
                if remaining_amount < 0.01:
                    break
        
        # 3.3 记录匹配结果
        if matched_orders:
            matches.append({
                'bank_record': bank_rec,
                'orders': matched_orders,
                'unmatched_amount': remaining_amount
            })
    
    return matches
```

### 3.2 AI分类优化算法

```python
def industry_classify(description, partner, history_data):
    """
    氧化加工行业智能分类
    
    优先级：
    1. 精确规则匹配
    2. 历史学习匹配
    3. AI模型推断
    """
    # 1. 精确规则匹配
    for keyword, category in INDUSTRY_RULES.items():
        if keyword in description.lower():
            return category, 1.0  # 置信度100%
    
    # 2. 历史学习匹配
    history_match = find_similar_in_history(description, partner, history_data)
    if history_match and history_match.confidence > 0.8:
        return history_match.category, history_match.confidence
    
    # 3. AI模型推断
    ai_result = call_glm4_classifier(description, partner, INDUSTRY_CONTEXT)
    return ai_result.category, ai_result.confidence
```

## 4. 用户界面设计

### 4.1 主菜单优化

```
========================================================================
             飞书财务小助手 V10.0 - 氧化加工厂专版
========================================================================

【快捷操作】
  00. 🚀 一键全自动处理（推荐）
  01. 📋 新建加工订单
  02. 💰 订单收款对账
  03. 📊 查看经营报表

【数据管理】
  10. 📥 导入银行流水
  11. 📤 导入加工订单
  12. 👥 管理往来单位
  13. 🏷️  管理费用分类

【报表查询】
  20. 📈 加工费收入明细
  21. 💸 外发成本统计
  22. 📦 原材料消耗统计
  23. 🤝 往来对账表
  24. 💹 月度利润表

【系统设置】
  30. ⚙️  系统配置
  31. 📚 加载示例数据
  32. 💾 备份与恢复
  33. 📖 操作教程

  99. 🚪 退出系统

请选择功能编号：
```

### 4.2 订单录入向导

```
┌─────────────────────────────────────────┐
│      新建加工订单 - 分步向导             │
└─────────────────────────────────────────┘

步骤 1/5：基本信息
─────────────────────────────────────────
客户名称：[华为科技▼]  (支持搜索和新建)
订单日期：[2026-02-09]
物品名称：[铝合金外壳]

步骤 2/5：计价信息
─────────────────────────────────────────
计价单位：[件▼] (件/条/只/个/米长/米重/平方)
数量：[1000]
单价：[2.5] 元
订单金额：2500.00 元 (自动计算)

步骤 3/5：工序信息
─────────────────────────────────────────
工序明细：[喷砂、氧化、封孔]
外发工序：☑ 喷砂  ☐ 拉丝  ☐ 抛光
外发成本：[300] 元

步骤 4/5：确认信息
─────────────────────────────────────────
客户：华为科技
物品：铝合金外壳 × 1000 件
单价：2.5 元/件
订单金额：2500.00 元
外发成本：300.00 元
预计利润：2200.00 元

[上一步]  [确认创建]  [取消]
```

### 4.3 对账操作界面

```
┌─────────────────────────────────────────┐
│      收款对账 - 华为科技                 │
└─────────────────────────────────────────┘

未对账银行流水：
┌────┬────────────┬──────────┬────────┐
│ 选 │ 日期       │ 金额     │ 备注   │
├────┼────────────┼──────────┼────────┤
│ ☑  │ 2026-02-09 │ 3000.00  │ 转账   │
│ ☐  │ 2026-02-10 │ 1500.00  │ 转账   │
└────┴────────────┴──────────┴────────┘

未收款订单：
┌────┬──────────────┬──────────┬──────────┬──────────┐
│ 选 │ 订单号       │ 订单金额 │ 已收款   │ 未收款   │
├────┼──────────────┼──────────┼──────────┼──────────┤
│ ☑  │ PO20260209001│ 2500.00  │ 0.00     │ 2500.00  │
│ ☑  │ PO20260209002│ 1800.00  │ 0.00     │ 1800.00  │
│ ☐  │ PO20260209003│ 3200.00  │ 0.00     │ 3200.00  │
└────┴──────────────┴──────────┴──────────┴──────────┘

对账预览：
  银行流水：3000.00 元
  订单总额：4300.00 元 (PO001: 2500 + PO002: 1800)
  对账金额：3000.00 元
  剩余未收：1300.00 元 (PO002剩余)

[自动匹配]  [手动调整]  [确认对账]  [取消]
```

## 5. 示例数据设计

### 5.1 示例订单数据

创建10个典型的加工订单，覆盖不同计价单位和工序组合：

1. 铝合金外壳 - 1000件 - 喷砂+氧化
2. 不锈钢管 - 500米长 - 拉丝+氧化
3. 铝型材 - 200公斤 - 抛光+氧化
4. 铝板 - 50平方米 - 氧化
5. 五金配件 - 5000只 - 喷砂+氧化+封孔
6. 铝棒 - 300条 - 氧化
7. 散热器 - 800个 - 拉丝+氧化
8. 铝框 - 1500件 - 氧化
9. 铝管 - 1000米长 - 抛光+氧化
10. 铝片 - 100公斤 - 氧化

### 5.2 示例银行流水

创建30条银行流水，包含：
- 客户付款（对应订单）
- 供应商付款（原材料、外发加工）
- 固定支出（房租、水电、工资）
- 多笔合并付款场景
- 分批付款场景

### 5.3 示例往来单位

创建15个往来单位：
- 5个客户（含别名）
- 8个供应商（原材料、外发加工）
- 2个既是客户又是供应商

## 6. 报表设计

### 6.1 加工费收入明细表

```
加工费收入明细表
统计期间：2026年2月

按客户统计：
┌──────────────┬────────┬──────────┬──────────┬──────────┐
│ 客户名称     │ 订单数 │ 订单金额 │ 已收款   │ 未收款   │
├──────────────┼────────┼──────────┼──────────┼──────────┤
│ 华为科技     │ 5      │ 12500.00 │ 10000.00 │ 2500.00  │
│ 小米科技     │ 3      │ 8600.00  │ 8600.00  │ 0.00     │
│ ...          │ ...    │ ...      │ ...      │ ...      │
├──────────────┼────────┼──────────┼──────────┼──────────┤
│ 合计         │ 15     │ 45000.00 │ 38000.00 │ 7000.00  │
└──────────────┴────────┴──────────┴──────────┴──────────┘

按计价单位统计：
┌──────────┬────────┬──────────┬──────────┐
│ 计价单位 │ 订单数 │ 总数量   │ 总金额   │
├──────────┼────────┼──────────┼──────────┤
│ 件       │ 8      │ 15000    │ 28000.00 │
│ 米长     │ 4      │ 2500     │ 10000.00 │
│ 平方     │ 2      │ 150      │ 5000.00  │
│ 米重     │ 1      │ 500      │ 2000.00  │
├──────────┼────────┼──────────┼──────────┤
│ 合计     │ 15     │ -        │ 45000.00 │
└──────────┴────────┴──────────┴──────────┘
```

### 6.2 外发成本统计表

```
外发加工成本统计表
统计期间：2026年2月

按工序统计：
┌──────────┬────────┬──────────┬──────────┐
│ 工序类型 │ 订单数 │ 外发次数 │ 总成本   │
├──────────┼────────┼──────────┼──────────┤
│ 喷砂     │ 8      │ 12       │ 3600.00  │
│ 拉丝     │ 5      │ 8        │ 2400.00  │
│ 抛光     │ 3      │ 5        │ 1500.00  │
├──────────┼────────┼──────────┼──────────┤
│ 合计     │ -      │ 25       │ 7500.00  │
└──────────┴────────┴──────────┴──────────┘

按供应商统计：
┌──────────────┬────────┬──────────┐
│ 供应商名称   │ 外发次数│ 总成本   │
├──────────────┼────────┼──────────┤
│ 鑫达喷砂厂   │ 12     │ 3600.00  │
│ 华美拉丝厂   │ 8      │ 2400.00  │
│ 精工抛光厂   │ 5      │ 1500.00  │
├──────────────┼────────┼──────────┤
│ 合计         │ 25     │ 7500.00  │
└──────────────┴────────┴──────────┘
```

## 7. 技术实现要点

### 7.1 模块化设计

```python
# 新增模块结构
oxidation_factory/
├── __init__.py
├── order_manager.py      # 订单管理
├── reconciliation.py     # 对账引擎
├── industry_classifier.py # 行业分类器
├── report_generator.py   # 报表生成
├── demo_data.py          # 示例数据生成
└── config.py             # 配置管理
```

### 7.2 配置管理

使用分层配置：
1. 默认配置（代码内置）
2. 行业配置（oxidation_config.json）
3. 用户配置（.env）

### 7.3 数据迁移

提供数据迁移脚本，将现有数据平滑升级到新结构。

## 8. 测试策略

### 8.1 单元测试

- 对账算法测试
- AI分类测试
- 数据验证测试

### 8.2 集成测试

- 完整业务流程测试
- 示例数据导入测试
- 报表生成测试

### 8.3 用户验收测试

- 小白会计操作测试
- 典型业务场景测试
- 性能压力测试

## 9. 部署方案

### 9.1 打包方案

使用PyInstaller打包为独立EXE：
```bash
pyinstaller --onefile --windowed --name="氧化加工厂财务助手" CW.py
```

### 9.2 配置文件部署

将配置文件和示例数据打包到安装目录：
```
Feishu_Finance_Assistant/
├── 氧化加工厂财务助手.exe
├── 财务数据/
│   ├── 配置文件/
│   │   ├── .env
│   │   ├── oxidation_config.json
│   │   └── category_rules.json
│   ├── Excel模版/
│   │   ├── 加工订单导入模板.xlsx
│   │   ├── 银行流水导入模板.xlsx
│   │   └── 往来单位导入模板.xlsx
│   └── 示例数据/
│       └── 氧化加工厂示例数据.xlsx
└── 使用手册_氧化加工厂版.txt
```

## 10. 正确性属性

### 属性 1：对账金额一致性
**描述**：对账后，订单的已收款金额之和应等于关联的银行流水金额之和。

**形式化**：
```
∀ reconciliation_record r:
  sum(r.orders.paid_amount) = sum(r.bank_records.amount)
```

### 属性 2：余额计算正确性
**描述**：往来单位的应收余额应等于所有未收款订单金额之和。

**形式化**：
```
∀ partner p:
  p.receivable_balance = sum(p.orders.unpaid_amount)
```

### 属性 3：分类规则优先级
**描述**：当多个分类规则匹配时，应选择优先级最高的规则。

**形式化**：
```
∀ transaction t, rules R:
  if multiple_match(t, R):
    selected_rule = max(R, key=lambda r: r.priority)
```

### 属性 4：数据完整性
**描述**：导入的订单必须包含所有必填字段。

**形式化**：
```
∀ order o:
  o.customer ≠ null ∧
  o.date ≠ null ∧
  o.amount > 0 ∧
  o.pricing_unit ∈ VALID_UNITS
```

### 属性 5：对账状态一致性
**描述**：已完全对账的订单，其未收款金额应为0。

**形式化**：
```
∀ order o:
  o.reconcile_status = '已对账' ⟹ o.unpaid_amount = 0
```

## 11. 性能优化

### 11.1 缓存策略

- 往来单位列表缓存（5分钟）
- AI分类结果缓存（永久，可手动刷新）
- 报表数据缓存（1分钟）

### 11.2 批量操作

- 批量导入订单（每批100条）
- 批量对账（每批50条）
- 批量更新状态（每批100条）

### 11.3 异步处理

- 文件监听异步处理
- 报表生成异步处理
- 备份操作异步处理

## 12. 安全性设计

### 12.1 数据验证

- 输入数据格式验证
- 金额范围验证
- 日期合理性验证

### 12.2 权限控制

- 操作日志记录
- 关键操作二次确认
- 数据修改历史追踪

### 12.3 备份策略

- 每日自动备份
- 关键操作前备份
- 保留30天历史备份

